control:
  keyboard:
    angle_step: 5
    enabled: true
    gripper_step: 10.0
    pos_step: 0.01
  pybullet:
    enabled: true
  vr:
    enabled: true
gripper:
  closed_angle: 45.0
  open_angle: 0.0
ik:
  use_reference_poses: true
  reference_poses_file: reference_poses.json
  position_error_threshold: 0.001
  hysteresis_threshold: 0.05
  movement_penalty_weight: 0.01
network:
  host_ip: 0.0.0.0
  https_port: 8443
  websocket_port: 8442
paths:
  urdf_path: URDF/SO100/urdf/so100.urdf
robot:
  type: xlerobot  # "so100" for 1-2 arms without wheels, "xlerobot" for full xlerobot with wheels
  left_arm:
    enabled: true
    name: Left Arm
    port: /dev/ttySO100blue
  right_arm:
    enabled: true
    name: Right Arm
    port: /dev/ttySO100red
  send_interval: 0.05
  vr_to_robot_scale: 1
base:
  mode: differential  # "differential" (2-wheel) or "omnidirectional" (3-wheel)
  differential:
    left_motor_id: 10
    right_motor_id: 9
    wheel_radius: 0.0625  # 6.25cm (12.5cm diameter)
    wheel_base_width: 0.52  # 52cm between wheels
  omnidirectional:
    left_motor_id: 7
    back_motor_id: 8
    right_motor_id: 9
    wheel_radius: 0.05  # 5cm
    wheel_base_radius: 0.125  # 12.5cm from center to each wheel
ssl:
  certfile: cert.pem
  keyfile: key.pem
