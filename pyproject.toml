[build-system]
requires = ["setuptools >= 61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "telegrip"
version = "1.0.0"
description = "Teleoperation Control System for the SO100 Robot Arm(s)"
authors = [
    {name = "Emil Rofors", email = "emirof@gmail.com"},
]
keywords = ["robotics", "teleoperation", "vr", "robot-control"]

dependencies = [
    "numpy",
    "websockets",
    "pynput",
    "pybullet",
    "scipy",
    "pyyaml",
]

requires-python = ">=3.8"

[project.optional-dependencies]
ml = [
    "torch",
    "lerobot[feetech]",
]
dev = [
    "pytest",
    "pytest-asyncio",
    "black",
    "flake8",
    "mypy",
]

[project.scripts]
telegrip = "telegrip.main:main_cli"

[project.urls]
Homepage = "https://github.com/Luis-Lundgren/telegrip"
Repository = "https://github.com/Luis-Lundgren/telegrip"
Upstream = "https://github.com/DipFlip/telegrip"
Issues = "https://github.com/Luis-Lundgren/telegrip/issues"

[tool.setuptools]
packages = ["telegrip"] 